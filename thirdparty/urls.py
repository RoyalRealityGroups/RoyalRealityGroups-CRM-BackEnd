from django.urls import path

from . import views

urlpatterns = [

    # path('focus_data_sync/', views.FocusDataSync.as_view()),
    # path('focus_data_sync/status/', views.FocusDataSyncStatus.as_view()),

    # path('sync_trigger/list/', views.SyncTriggerList.as_view()),
    # path('sync_trigger/details/<str:pk>/', views.SyncTriggerDetails.as_view()),

    # path('sync_log/list/', views.SyncLogList.as_view()),

]